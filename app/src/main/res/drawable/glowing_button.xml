<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed State -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="@color/cyan_bright"/>
            <corners android:radius="25dp"/>
        </shape>
    </item>

    <!-- Default State -->
    <item>
        <shape>
            <solid android:color="@color/cyan_glow"/>
            <corners android:radius="25dp"/>
        </shape>
    </item>
</selector>
